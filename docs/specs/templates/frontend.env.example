# この機能の Vue 用。src/features/<feature-name>/frontend/.env にコピーして値を埋める。

# この機能の FastAPI の基点（末尾スラッシュなし）
# 名前は VITE_API_<FEATURE>_URL（機能名を大文字、ハイフンはアンダースコア）
# コピー後、機能名に合わせて変数名を変えること。
# 例: 機能名が voice なら VITE_API_VOICE_URL=http://localhost:8000
VITE_API_VOICE_URL=http://localhost:8000
