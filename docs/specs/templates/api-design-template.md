# API設計: ＜機能名＞

## 概要

この機能の FastAPI が公開する HTTP API の契約。`requirements.md` の該当 REQ を満たすことだけを書く。テーブル定義は `db-design.md` を参照する。

関連ドキュメント:

- 全体設計: `design.md`
- DB設計: `db-design.md`

## 共通

### 認証

認証しない場合は「認証しない」と書く。必要な場合のみ方式を書き、エンドポイントごとに要/不要を示す。

### エラー（共通）

| 状況 | 応答 |
|------|------|
| 入力不正 | 400 |
| 対象なし | 404 |
| 未処理例外 | 500、内部情報を本文に含めない |

## エンドポイント一覧

| メソッド | パス | 認証 | 対応 REQ |
|----------|------|------|----------|
| ＜METHOD＞ | `/＜path＞` | 要 / 不要 | REQ-001 |

## エンドポイント

### ＜METHOD＞ `/＜path＞`

- 認証: 要 / 不要
- 対応 REQ: REQ-001

要求: ＜なし、または JSON の形＞

応答: ＜HTTP ステータス＞ ＜JSON の形＞

処理概要: ＜何をするか＞

エラー:

| 状況 | 応答 |
|------|------|
| ＜状況＞ | ＜HTTP ステータス＞ |

### ＜METHOD＞ `/＜path＞`（続く場合）

- 認証: 要 / 不要
- 対応 REQ: REQ-002

要求: ＜なし、または JSON の形＞

応答: ＜HTTP ステータス＞ ＜JSON の形＞

処理概要:

エラー:

| 状況 | 応答 |
|------|------|
| ＜状況＞ | ＜HTTP ステータス＞ |

## 要件トレーサビリティ

| 要件 | 設計 |
|------|------|
| REQ-001 | ＜METHOD /path＞ |

## 未決事項

-

## 承認

現在の状態: 未承認

| 日時 | 状態 | 変更概要 |
|------|------|----------|
| ＜YYYY-MM-DD HH:mm＞ | 未承認 | 初版 |
