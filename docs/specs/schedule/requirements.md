# 機能: schedule（スケジュール管理）

## 目的

利用者が自分の予定と TODO をカレンダー上で登録・確認・更新できるようにする。種類・日時の粒度・カテゴリ（色）と、自分用の休日を整え、日単位と時間単位を区別して管理する。毎月繰り返す内容はルーチンとして持ち、指定した年月へ適用してスケジュールを作れるようにする。

## ユーザーストーリー

- 利用者として、予定と TODO をカレンダーで見たい。なぜなら、いつ何があるかを一目で把握したいから。
- 利用者として、予定と TODO を追加・変更・削除したい。なぜなら、自分の予定を最新に保ちたいから。
- 利用者として、TODO の実施済み／未実施を管理したい。なぜなら、やり残しが分かるようにしたいから。
- 利用者として、カテゴリを自分用に追加・変更・削除し、色を付けたい。なぜなら、種類ごとに見分けたいから。
- 利用者として、見たくないカテゴリを非表示にしたい。なぜなら、今見たいものだけに集中したいから。
- 利用者として、日曜始まりと月曜始まりを切り替えたい。なぜなら、自分の週の感覚に合わせたいから。
- 利用者として、PC とスマートフォンでカレンダーの見せ方を変えたい。なぜなら、画面の広さに合わせて確認したいから。
- 利用者として、今日の月へ戻りたい。なぜなら、別の月を見たあとすぐ現在に戻したいから。
- 利用者として、自分用の休日を年月日と名称で登録したい。なぜなら、会社の休日など、国民の祝日以外もカレンダーで見分けたいから。
- 利用者として、毎月繰り返す予定や TODO をルーチンとして登録・変更したい。なぜなら、同じ内容を毎月手で入れなくてよいから。
- 利用者として、ルーチンを指定した年月へ適用したい。なぜなら、その月の予定をまとめて作りたいから。
- 利用者として、祝日や特定の曜日に当たるときは前後へずらしたい。なぜなら、休業日を避けて実施したいから。

## 用語

| 用語 | 意味 |
|------|------|
| スケジュール | 利用者が登録する一件。種別は「予定」または「TODO」。 |
| 予定 | 実施状態を持たないスケジュール。 |
| TODO | 実施済み／未実施を持つスケジュール。 |
| 日単位 | 年月日までで開始と終了を管理する粒度。時刻は持たない。 |
| 時間単位 | 年月日時分までで開始と終了を管理する粒度。秒は持たない。 |
| カテゴリ | 利用者本人が持つ分類。名称と色を持つ。 |
| 論理削除 | データを残したまま、以降の通常の対象外とすること。物理削除は行わない。 |
| 表示対象月 | カレンダーに出している年月。 |
| 週の開始曜日 | カレンダーの左端を日曜にするか月曜にするかの指定。 |
| カテゴリ非表示 | そのカテゴリのスケジュールをカレンダーに出さないこと。カテゴリ自体は削除しない。 |
| ユーザ休日 | 利用者本人が登録する休日。年月日と名称を持つ。日本の祝日とは別である。 |
| ルーチン | 毎月繰り返す予定または TODO のひな型。適用すると、指定年月のスケジュールになる。 |
| ルーチン識別 | ルーチンごとに付与する識別。適用で作ったスケジュールに付ける。手入力のスケジュールには無い。 |
| 適用日タイプ | ルーチンの基準日の決め方。日付指定または曜日指定。 |
| 日付指定 | 月末日、または毎月 X 日で基準日を決めること。 |
| 曜日指定 | その月の N 番目の X 曜日、または最終から N 番目の X 曜日で基準日を決めること。 |
| 反映月 | ルーチンを適用する対象の月。1 月〜12 月から複数選べる。 |
| 除外調整日 | 基準日が祝日または選んだ曜日に当たるとき、前後へずらすこと。 |
| 基準日 | 適用日タイプと指定年月から決めた、ずらす前の年月日。 |
| 適用日 | 除外調整のあと、スケジュールの開始と終了にする年月日。 |
| 本機能 | このスケジュール管理機能そのもの。メニューへ載せる機能の一つである。 |
| ログ | 操作の入力、判断結果、正常系ではない場合の理由を、タイムスタンプと区分付きで残す記録。画面へ出す文言とは別である。 |

## 要件

要件は EARS の意味を保ち、日本語の文で書く。画面部品、テーブル定義、API パス、HTTP ステータスなど設計は書かない。

### REQ-001

本機能を利用するとき、システムはログインを必須とし、ログイン中ユーザに本機能が割り当てられているときだけ利用を許可すること。

#### 受け入れ条件

- [ ] 未ログインでは、スケジュールとカテゴリとユーザ休日とルーチンを参照・操作できない。
- [ ] 本機能が割り当てられていないログイン中ユーザは、参照・操作できない。
- [ ] 論理削除済みのユーザまたは本機能では、参照・操作できない。
- [ ] 判定は他機能の API に依存しない。

### REQ-002

システムは、スケジュール・カテゴリ・ユーザ休日・ルーチン・表示設定をユーザごとに保持し、他ユーザのデータは参照させないこと。

#### 受け入れ条件

- [ ] あるユーザのスケジュール・カテゴリ・ユーザ休日・ルーチン・表示設定は、そのユーザ自身だけが参照・操作できる。
- [ ] 他ユーザのスケジュール・カテゴリ・ユーザ休日・ルーチン・表示設定は見えない。

### REQ-003

システムは、スケジュールを「予定」または「TODO」のいずれかとして管理すること。TODO だけが実施済み／未実施を持つこと。

#### 受け入れ条件

- [ ] 予定として登録できる。
- [ ] TODO として登録できる。
- [ ] 予定は実施状態を持たない。
- [ ] TODO は実施済みまたは未実施のいずれかである。
- [ ] 種別を予定と TODO の間で変更できる。
- [ ] 予定から TODO に変えたとき、実施状態は未実施になる。
- [ ] TODO から予定に変えたとき、実施状態は持たなくなる。

### REQ-004

システムは、スケジュールに次を保持すること。タイトル、開始、終了、カテゴリ、場所、詳細、日単位か時間単位か、種別、ルーチン識別。TODO のときは実施状態も保持すること。ルーチン識別は無くてもよい。

#### 受け入れ条件

- [ ] 上記の項目を保持できる。
- [ ] タイトルは必須である。
- [ ] 開始と終了は必須である。
- [ ] カテゴリは必須である。未削除の、本人のカテゴリだけを指定できる。
- [ ] 場所と詳細は空でもよい。
- [ ] ルーチン識別は空（無い）でもよい。初期状態は無い。
- [ ] 画面から手入力で追加したスケジュールのルーチン識別は無い。

### REQ-005

利用者が日単位を選んだとき、システムは開始と終了を年月日で管理すること。時間単位を選んだとき、システムは開始と終了を年月日時分で管理すること。

#### 受け入れ条件

- [ ] 日単位の開始・終了に時刻は付かない。
- [ ] 時間単位の開始・終了は分までであり、秒は持たない。
- [ ] 粒度を日単位と時間単位の間で変更できる。

### REQ-006

終了が開始より前になる入力のとき、システムは登録および更新を拒否すること。

#### 受け入れ条件

- [ ] 日単位で終了日が開始日より前のときは失敗する。
- [ ] 時間単位で終了日時が開始日時より前のときは失敗する。
- [ ] 開始と終了が同じときは受け付ける。

### REQ-007

ログイン中の、本機能が割り当てられた利用者がスケジュールを追加するとき、システムは当該利用者のスケジュールとして追加すること。

#### 受け入れ条件

- [ ] タイトル、開始、終了、カテゴリ、粒度、種別を指定して追加できる。場所と詳細は省略できる。
- [ ] TODO として追加したときの実施状態は未実施である。
- [ ] 画面から手入力で追加したときのルーチン識別は無い。
- [ ] 追加したスケジュールは、当該利用者のカレンダーに出る。

### REQ-008

ログイン中の、本機能が割り当てられた利用者がスケジュールを更新するとき、システムは当該利用者の未削除スケジュールを更新できること。

#### 受け入れ条件

- [ ] 未削除の自分のスケジュールの項目を更新できる。
- [ ] 更新してもルーチン識別は変えない。
- [ ] 存在しない、論理削除済み、または他ユーザのスケジュールの更新は失敗する。

### REQ-009

ログイン中の、本機能が割り当てられた利用者がスケジュールを削除するとき、システムは当該スケジュールを論理削除すること。

#### 受け入れ条件

- [ ] 指定した自分の未削除スケジュールを論理削除できる。
- [ ] 削除したスケジュールはカレンダーに出ない。
- [ ] 削除後もデータは残る。物理削除はしない。
- [ ] 存在しない、既に論理削除済み、または他ユーザのスケジュールの削除は失敗する。

### REQ-010

ログイン中の、本機能が割り当てられた利用者が TODO の実施状態を変えるとき、システムは当該 TODO の実施済み／未実施を更新すること。

#### 受け入れ条件

- [ ] 未実施の TODO を実施済みにできる。
- [ ] 実施済みの TODO を未実施にできる。
- [ ] 予定の実施状態は変えられない。
- [ ] 画面が広いときは、日セル上からも実施状態を変えられる。
- [ ] 画面が狭いときは、詳細からは実施状態を変えられず、編集で変える。

### REQ-011

ログイン中の、本機能が割り当てられた利用者がカテゴリを追加するとき、システムは当該利用者のカテゴリとして、名称と色を付けて追加すること。同一ユーザの未削除カテゴリと名称が重複するときは、追加を拒否すること。

#### 受け入れ条件

- [ ] 名称と色を指定して追加できる。
- [ ] 名称は空では受け付けない。
- [ ] 同一ユーザの未削除カテゴリに同じ名称があるときは失敗する。
- [ ] 他ユーザが同じ名称を使っていても追加できる。
- [ ] 論理削除済みのカテゴリと同じ名称なら追加できる。
- [ ] 追加したカテゴリは、当該利用者のカテゴリ一覧に出る。
- [ ] 他ユーザのカテゴリ一覧には出ない。

### REQ-012

ログイン中の、本機能が割り当てられた利用者がカテゴリを更新するとき、システムは当該利用者の未削除カテゴリの名称と色を更新できること。変更後の名称が同一ユーザの別の未削除カテゴリと重複するときは、更新を拒否すること。

#### 受け入れ条件

- [ ] 未削除の自分のカテゴリの名称を変えられる。
- [ ] 未削除の自分のカテゴリの色を変えられる。
- [ ] 名称を空にはできない。
- [ ] 変更後の名称が、同一ユーザの別の未削除カテゴリと同じときは失敗する。
- [ ] 名称を変えずに色だけ変えるときは、同じ名称のままで成功する。
- [ ] 存在しない、論理削除済み、または他ユーザのカテゴリの更新は失敗する。

### REQ-013

ログイン中の、本機能が割り当てられた利用者がカテゴリを削除するとき、システムは当該カテゴリを論理削除すること。紐づくスケジュールは残し、カテゴリとの対応は維持すること。

#### 受け入れ条件

- [ ] 指定した自分の未削除カテゴリを論理削除できる。
- [ ] 削除後もカテゴリのデータは残る。物理削除はしない。
- [ ] そのカテゴリを付けた未削除スケジュールは残る。
- [ ] 存在しない、既に論理削除済み、または他ユーザのカテゴリの削除は失敗する。

### REQ-014

ログイン中の、本機能が割り当てられた利用者が、削除済みカテゴリを一覧に出すかどうかを切り替えたとき、システムはその指定に従うこと。指定は当該利用者の設定として保持すること。

#### 受け入れ条件

- [ ] 出さない指定のとき、削除済みカテゴリは一覧に出ない。
- [ ] 出す指定のとき、削除済みカテゴリが一覧に出る。
- [ ] 指定は再表示しても維持される。
- [ ] 初期状態では、削除済みカテゴリは一覧に出さない。

### REQ-015

ログイン中の、本機能が割り当てられた利用者がカテゴリごとに表示／非表示を切り替えたとき、システムは非表示にしたカテゴリのスケジュールをカレンダーに出さないこと。指定は当該利用者の設定として保持すること。

#### 受け入れ条件

- [ ] 非表示にしたカテゴリの予定と TODO はカレンダーに出ない。
- [ ] 表示に戻すと、そのカテゴリの未削除スケジュールが再び出る。
- [ ] 指定は再表示しても維持される。
- [ ] 初期状態では、未削除カテゴリはすべて表示である。
- [ ] 削除済みカテゴリを一覧に出さないとき、そのカテゴリのスケジュールもカレンダーに出ない。

### REQ-016

ログイン中の、本機能が割り当てられた利用者がカレンダーを開いたとき、システムは表示対象月のカレンダーを示すこと。前月と翌月へ移れること。

#### 受け入れ条件

- [ ] 開いた直後は、操作している日の年月を表示対象月とする。
- [ ] 表示対象月の年月が分かる。
- [ ] 前月へ移れる。
- [ ] 翌月へ移れる。
- [ ] 表示対象月に属する日と、その月の週を埋める前後の日が出る。
- [ ] 表示対象月の日が 1 日も無い週は出さない。
- [ ] 表示対象月の日を 1 日でも含む週は、週内の前後の月の日も含めて出す。

### REQ-017

ログイン中の、本機能が割り当てられた利用者が週の開始曜日を日曜始まりまたは月曜始まりに切り替えたとき、システムはカレンダーの並びをそれに合わせること。指定は当該利用者の設定として保持すること。

#### 受け入れ条件

- [ ] 日曜始まりにできる。
- [ ] 月曜始まりにできる。
- [ ] 指定は再表示しても維持される。
- [ ] 初期状態は日曜始まりである。

### REQ-018

システムは、カレンダーの各日に、その日が日本の祝日であるときは祝日名称を付けること。

#### 受け入れ条件

- [ ] 祝日の日には祝日名称が出る。
- [ ] 祝日でない日には祝日名称は出ない。
- [ ] 振替休日を含む。

### REQ-019

システムは、スケジュールを次の順で示すこと。日単位、時間単位の順。同じ粒度の中では開始の昇順。開始が同じときは終了の昇順。終了も同じときはタイトルの辞書順。

#### 受け入れ条件

- [ ] 同じ日に日単位と時間単位があるとき、日単位が先である。
- [ ] 開始が早いものが先である。
- [ ] 開始が同じときは、終了が早いものが先である。
- [ ] 開始も終了も同じときは、タイトルの昇順である。

### REQ-020

開始から終了までに含まれる各日に、システムは当該スケジュールを示すこと。

#### 受け入れ条件

- [ ] 開始日と終了日が同じときは、その日に出る。
- [ ] 開始日と終了日が異なるときは、その間の各日に出る。
- [ ] 表示対象月の外の日にしかかからないスケジュールは、その月の日セルには出ない。

### REQ-021

画面が広いとき（PC 向け）、システムはカテゴリの操作領域と月カレンダーを同時に示すこと。ユーザ休日の追加と削除は、日を右クリックしたメニューで行うこと。ヘッダの設定は週の開始の切替と、ルーチンの一覧・追加・削除・適用とする。

#### 受け入れ条件

- [ ] カテゴリ一覧が見える。
- [ ] カテゴリの追加・名称変更・色変更・削除ができる。
- [ ] カテゴリごとの表示／非表示を切り替えられる。
- [ ] 削除済みカテゴリを一覧に出すかどうかを切り替えられる。
- [ ] ヘッダに設定がある。
- [ ] 設定を選ぶと、週の開始の切替とルーチンの一覧が出る。
- [ ] 設定のダイアログにユーザ休日の一覧・追加は無い。
- [ ] ユーザ休日でない日を右クリックすると、祝日として登録する操作が出る。
- [ ] ユーザ休日である日を右クリックすると、祝日から削除する操作が出る。
- [ ] 月カレンダーが見える。

### REQ-022

画面が広いとき、システムは各日に次を示すこと。日付、日本の祝日名称（あるとき）、ユーザ休日の名称（あるとき）、その日の表示対象スケジュール。日付の文字色は、土曜日を青、日曜日および休日（日本の祝日またはユーザ休日）を赤とする。休日と土曜日が重なるときは赤とする。日単位はタイトル。時間単位は、開始日では開始時刻（時分）とタイトル、開始日以外ではタイトルのみ。TODO は実施状態が分かり、実施済みは取り消し線で示す。各スケジュールには、そのカテゴリの色を背景の帯として付ける。表示しきれないときは、残件数を示すこと。

#### 受け入れ条件

- [ ] 日付が出る。
- [ ] 日本の祝日のときはその名称が出る。
- [ ] ユーザ休日のときはその名称が出る。
- [ ] 両方ある日は、両方の名称が出る。
- [ ] 土曜日の日付は青である。日曜日の日付は赤である。
- [ ] 日本の祝日またはユーザ休日の日付は赤である。土曜日と重なるときも赤である。
- [ ] 日単位はタイトルで出る。
- [ ] 時間単位の開始日では `HH:MM` とタイトルで出る。開始時刻はそのスケジュールの開始の時分である。
- [ ] 時間単位の開始日以外ではタイトルのみで出る。
- [ ] TODO は予定と区別でき、実施状態をその場で変えられる。
- [ ] 実施済みの TODO は取り消し線で出る。
- [ ] カテゴリの色が帯として付く。
- [ ] 表示しきれない件数があるとき、残件数が分かる。
- [ ] 残件数から、その日の表示しきれていないスケジュールを参照できる。

### REQ-023

画面が広いとき、利用者が日付を選んだときはシステムは新規スケジュールの追加を受け付けること。表示中のスケジュールを選んだとき、および残件数から出したスケジュールを選んだときは、その編集を受け付けること。日付を選んで追加するときは、開始と終了の初期値をその日とすること。

#### 受け入れ条件

- [ ] 日付を選ぶと、新規追加の入力を始められる。
- [ ] 新規追加の開始と終了の初期値は、選んだ日である。
- [ ] 表示中のスケジュールを選ぶと、その編集を始められる。
- [ ] 残件数から出したスケジュールを選ぶと、その編集を始められる。

### REQ-024

画面が狭いとき（スマートフォン向け）、システムは月カレンダーと、日付を選んだときだけ出す詳細を示すこと。詳細に出しきれないときは、詳細の中をスクロールできること。

#### 受け入れ条件

- [ ] 年月と月カレンダーが見える。
- [ ] 日付を選ぶ前は、詳細は出ない。
- [ ] 日付を選ぶと、その日の詳細が出る。
- [ ] 詳細に出しきれないときは、詳細の中をスクロールして残らず見られる。

### REQ-025

画面が狭いとき、システムはスケジュールがある日に、その日の表示対象スケジュールのカテゴリ色を点で示すこと。日付の文字色は REQ-022 と同じ規則とする。詳細には、選んだ日の年月日と、日本の祝日名称およびユーザ休日の名称（あるとき）と、その日の表示対象スケジュールを出すこと。時間単位は、開始日では開始時分から終了時分とタイトル、開始日以外ではタイトルのみ。日単位はタイトル。

#### 受け入れ条件

- [ ] 表示対象のスケジュールがある日に、該当カテゴリの色の点が出る。
- [ ] 表示対象のスケジュールが無い日には、点は出ない。
- [ ] 土曜日の日付は青、日曜日および休日の日付は赤である。
- [ ] 詳細の先頭に年月日が出る。祝日・ユーザ休日の名称があるときは付ける。
- [ ] 時間単位の開始日では `HH:MM ～ HH:MM` とタイトルで出る。
- [ ] 時間単位の開始日以外ではタイトルのみで出る。
- [ ] 日単位はタイトルで出る。
- [ ] 並びは REQ-019 に従う。
- [ ] TODO は予定と区別でき、実施済みは取り消し線で出る。
- [ ] 詳細から実施状態だけを変えることはできない。

### REQ-026

画面が狭いとき、利用者が詳細のスケジュールを選んだときはシステムはその編集を受け付けること。新規追加の操作を選んだときは、新規スケジュールの追加を受け付けること。

#### 受け入れ条件

- [ ] 詳細のスケジュールを選ぶと、その編集を始められる。実施状態の変更も編集で行う。
- [ ] 新規追加の操作を選ぶと、新規追加の入力を始められる。
- [ ] 日付を選んだ状態で新規追加するとき、開始と終了の初期値はその日である。
- [ ] 日付を選んでいない状態で新規追加するとき、開始と終了の初期値は操作している日である。

### REQ-027

スケジュール・カテゴリ・ユーザ休日・ルーチン・表示設定の操作が試みられたとき、システムは当該試行の記録を log フォルダへ出力すること。

#### 受け入れ条件

- [ ] 追加・更新・削除・実施状態の変更・表示設定の変更・ユーザ休日の変更・ルーチンの追加と削除と適用の試行が log フォルダ配下のファイルに残る。
- [ ] 記録にはタイムスタンプが含まれる。
- [ ] 記録には区分（INF、WRN、ERR、DBG のいずれか）が含まれる。
- [ ] 記録には、入力のうち判断に使う値が含まれる。
- [ ] 記録には判断結果（成功または失敗）が含まれる。
- [ ] 失敗のとき、画面へ出さない内部理由が記録に含まれる。
- [ ] ファイルはサイズが大きくなったら新しいファイルへ切り替わる。

### REQ-028

ログイン中の、本機能が割り当てられた利用者がカレンダーの日を右クリックしてユーザ休日を追加するとき、システムは当該日を当該利用者の休日として、名称を付けて追加すること。同一ユーザの未削除のユーザ休日と年月日が重複するときは、追加を拒否すること。画面が狭いときは、日の長押しで同じ操作ができること。

#### 受け入れ条件

- [ ] ユーザ休日でない日を右クリック（画面が狭いときは長押し）すると、祝日として登録する操作が出る。
- [ ] 名称を入力して確定すると、その日に追加できる。
- [ ] 名称は空では受け付けない。
- [ ] 同一ユーザの未削除のユーザ休日に同じ年月日があるときは失敗する。
- [ ] 他ユーザが同じ年月日を使っていても追加できる。
- [ ] 論理削除済みのユーザ休日と同じ年月日なら追加できる。
- [ ] 日本の祝日と同じ年月日でも追加できる。
- [ ] 追加したユーザ休日は、当該利用者のカレンダーに出る。
- [ ] 他ユーザのカレンダーには出ない。

### REQ-029

ログイン中の、本機能が割り当てられた利用者がユーザ休日を更新するとき、システムは当該利用者の未削除ユーザ休日の年月日と名称を更新できること。変更後の年月日が同一ユーザの別の未削除ユーザ休日と重複するときは、更新を拒否すること。

#### 受け入れ条件

- [ ] 未削除の自分のユーザ休日の年月日を変えられる。
- [ ] 未削除の自分のユーザ休日の名称を変えられる。
- [ ] 名称を空にはできない。
- [ ] 変更後の年月日が、同一ユーザの別の未削除ユーザ休日と同じときは失敗する。
- [ ] 年月日を変えずに名称だけ変えるときは成功する。
- [ ] 存在しない、論理削除済み、または他ユーザのユーザ休日の更新は失敗する。

### REQ-030

ログイン中の、本機能が割り当てられた利用者が、ユーザ休日である日を右クリックして祝日から削除するとき、システムは当該ユーザ休日を論理削除すること。画面が狭いときは、日の長押しで同じ操作ができること。日本の祝日だけの日は、祝日から削除できないこと。

#### 受け入れ条件

- [ ] ユーザ休日である日を右クリック（画面が狭いときは長押し）すると、祝日から削除する操作が出る。
- [ ] 祝日から削除すると、指定した自分の未削除ユーザ休日を論理削除できる。
- [ ] 削除したユーザ休日はカレンダーに出ない。
- [ ] 日本の祝日だけの日には、祝日から削除する操作は出ない。
- [ ] 削除後もデータは残る。物理削除はしない。
- [ ] 存在しない、既に論理削除済み、または他ユーザのユーザ休日の削除は失敗する。

### REQ-031

システムは、ルーチンごとに識別を付与して管理すること。

#### 受け入れ条件

- [ ] 追加したルーチンには識別が付く。
- [ ] 識別は利用者の入力ではない。
- [ ] 同じ利用者の別ルーチンと識別は重ならない。

### REQ-032

システムは、ルーチンに次を保持すること。タイトル、カテゴリ、種別（予定または TODO）、適用日タイプ、反映月、除外調整日の有無、詳細メモ。適用日タイプが日付指定のときは、月末日か毎月 X 日かも保持すること。適用日タイプが曜日指定のときは、N 番目か最終から N 番目か、対象の曜日、N の値も保持すること。除外調整日が有のときは、除外する対象（日本の祝日、日曜、月曜、火曜、水曜、木曜、金曜、土曜から複数可）と、ずらしかた（前にずらす、または後にずらす）も保持すること。

#### 受け入れ条件

- [ ] 上記の項目を保持できる。
- [ ] タイトルは必須である。
- [ ] カテゴリは必須である。未削除の、本人のカテゴリだけを指定できる。
- [ ] 種別は予定または TODO のいずれかである。
- [ ] 適用日タイプは日付指定または曜日指定のいずれかである。
- [ ] 日付指定のときは、月末日または毎月 X 日のいずれかである。毎月 X 日の X は 1 以上 31 以下である。
- [ ] 曜日指定のときは、N 番目の X 曜日または最終から N 番目の X 曜日のいずれかである。N は 1 以上 5 以下である。曜日は日曜から土曜のいずれかである。
- [ ] 反映月は 1 月〜12 月から 1 つ以上である。複数選べる。
- [ ] 詳細メモは空でもよい。
- [ ] 除外調整日が無のときは、除外対象とずらしかたは使わない。
- [ ] 除外調整日が有のときは、除外対象を 1 つ以上選び、ずらしかたは前または後のいずれかである。

### REQ-033

ログイン中の、本機能が割り当てられた利用者がルーチンを追加するとき、システムは当該利用者のルーチンとして追加すること。

#### 受け入れ条件

- [ ] REQ-032 の必須項目を指定して追加できる。
- [ ] 追加したルーチンは、当該利用者のルーチン一覧に出る。
- [ ] 他ユーザのルーチン一覧には出ない。
- [ ] タイトルが空、カテゴリが未指定、反映月が 0 件、日付指定なのに日の決め方が無い、曜日指定なのに曜日の決め方が無い、除外調整日が有なのに除外対象が 0 件またはずらしかたが無いときは、追加を拒否する。

### REQ-034

ログイン中の、本機能が割り当てられた利用者がルーチンを削除するとき、システムは当該ルーチンを論理削除すること。紐づくスケジュールは残し、ルーチン識別との対応は維持すること。

#### 受け入れ条件

- [ ] 指定した自分の未削除ルーチンを論理削除できる。
- [ ] 削除後もルーチンのデータは残る。物理削除はしない。
- [ ] そのルーチン識別が付いた未削除スケジュールは残る。
- [ ] 削除したルーチンは一覧に出ない。
- [ ] 存在しない、既に論理削除済み、または他ユーザのルーチンの削除は失敗する。

### REQ-035

ログイン中の、本機能が割り当てられた利用者がヘッダの設定を選んだとき、システムは未削除のルーチン一覧を示すこと。追加、変更、削除、選んだルーチンの適用、全ルーチンの一括適用もできること。画面の広さによらない。

#### 受け入れ条件

- [ ] 設定を選ぶと、本人の未削除ルーチンが一覧に出る。
- [ ] 一覧から追加を始められる。
- [ ] 一覧から変更を始められる。
- [ ] 一覧から削除できる。
- [ ] 一覧からルーチンを選んで適用できる。
- [ ] 全ルーチンを一括適用できる。
- [ ] 論理削除済みのルーチンは一覧に出ない。
- [ ] 画面が狭いときも、同じ設定から一覧・追加・変更・削除・適用ができる。

### REQ-036

ログイン中の、本機能が割り当てられた利用者がルーチンを選んで適用するとき、システムは指定された年月に、当該ルーチンの内容でスケジュールを登録すること。登録するスケジュールには当該ルーチン識別を付けること。指定年月に、同一のルーチン識別が付いた未削除スケジュールが既にあるときは、登録しないこと。

#### 受け入れ条件

- [ ] 適用するときに年月を指定する。
- [ ] 指定年月の月が当該ルーチンの反映月に含まれないときは、登録しない。
- [ ] 指定年月に、同じルーチン識別が付いた未削除スケジュールが既にあるときは、登録しない。失敗にはしない。
- [ ] 登録するときは、適用日を開始かつ終了とする日単位のスケジュールである。
- [ ] タイトル、カテゴリ、種別はルーチンの値である。詳細は詳細メモである。場所は空である。
- [ ] 種別が TODO のとき、実施状態は未実施である。
- [ ] ルーチン識別が付く。
- [ ] カテゴリが論理削除済み、または本人のものでないときは、登録しない。
- [ ] 基準日または適用日を決められないときは、登録しない。

### REQ-037

ログイン中の、本機能が割り当てられた利用者が全ルーチンを一括適用するとき、システムは本人の未削除ルーチンのそれぞれについて、REQ-036 と同じ規則で指定年月へ適用すること。

#### 受け入れ条件

- [ ] 一括適用するときに年月を指定する。
- [ ] 未削除の全ルーチンを対象とする。
- [ ] あるルーチンが登録しない条件に当たっても、他のルーチンの適用は続ける。
- [ ] 論理削除済みのルーチンは対象にしない。

### REQ-038

適用するとき、システムは指定年月と適用日タイプから基準日を決めること。

#### 受け入れ条件

- [ ] 日付指定かつ月末日のとき、基準日はその月の最終日である。
- [ ] 日付指定かつ毎月 X 日のとき、その月に X 日があればそれを基準日とする。その月に X 日が無ければ基準日は無しとする。
- [ ] 曜日指定かつ N 番目の X 曜日のとき、その月の N 番目の当該曜日を基準日とする。その月に N 番目が無ければ基準日は無しとする。
- [ ] 曜日指定かつ最終から N 番目の X 曜日のとき、その月の最後から N 番目の当該曜日を基準日とする。その月に該当が無ければ基準日は無しとする。

### REQ-039

除外調整日が無のとき、システムは基準日を適用日とすること。除外調整日が有のとき、システムは基準日が除外対象に当たるあいだ、ずらしかたに従い 1 日ずつずらして適用日を決めること。祝日除外の対象は日本の祝日（振替休日を含む）とする。ユーザ休日は含めない。

#### 受け入れ条件

- [ ] 除外調整日が無のとき、適用日は基準日である。
- [ ] 除外調整日が有でも、基準日が除外対象でなければ、適用日は基準日である。
- [ ] 除外調整日が有で基準日が除外対象のとき、前にずらすなら 1 日ずつ前へ、後にずらすなら 1 日ずつ後へ進み、除外対象でない日を適用日とする。
- [ ] ずらした結果が指定年月の外になっても、その年月日を適用日とする。
- [ ] 31 日ずらしても除外対象のままなら、適用日は無しとする。

### REQ-040

ログイン中の、本機能が割り当てられた利用者がルーチンを更新するとき、システムは当該利用者の未削除ルーチンの項目を更新できること。紐づくスケジュールは変えず、ルーチン識別との対応は維持すること。

#### 受け入れ条件

- [ ] 未削除の自分のルーチンについて、REQ-032 の項目を変えられる。
- [ ] REQ-033 と同じ欠落・組合せのときは、更新を拒否する。
- [ ] そのルーチン識別が付いた未削除スケジュールの内容は変わらない。
- [ ] 更新後の内容は一覧に出る。
- [ ] 更新後に適用すると、更新後の内容でスケジュールを登録する。
- [ ] 存在しない、既に論理削除済み、または他ユーザのルーチンの更新は失敗する。

## 非機能要件

- 本機能の利用には認証を必須とする。利用可否は、ログイン中ユーザに本機能が割り当てられているかで判定する。他機能の API は使わない。
- スケジュール・カテゴリ・ユーザ休日・ルーチン・表示設定は、操作しているユーザ自身のものだけを対象とする。
- 削除は論理削除とし、物理削除はしない。
- 本機能は `portal` の Python を import しない。ログイン画面は本機能では提供しない（`portal` でログインする）。
- 操作の記録は、入力された内容、それを元にした判断結果、正常系ではない場合の理由を契機として出力する。
- ログはファイルサイズでローテーションする。上限サイズと世代数は、機能の `.env` で指定する。コードに埋め込まない。
- パスワードとセッション ID はログに出さない。
- 失敗時に画面へ出す文言は、内部の詳細な理由を含まない。内部理由はログにだけ残す。
- 祝日は日本の祝日（振替休日を含む）とする。ユーザ休日はそれとは別に、利用者本人が登録する。
- 日時の基準は日本標準時とする。

## 対象外

- ログイン画面、メニュー画面（`portal` が担う）
- 本機能の初期登録（機能マスタへの追加と、利用者への割当は運用コマンドで行う）
- 他ユーザとのスケジュール共有、公開、招待
- 毎週同じ曜日へ出すこと（月内の第 N 曜日・月末以外の週次繰り返し）
- ルーチンの論理削除の取り消し
- 適用で作ったスケジュールの一括取り消し
- 時間単位のルーチン（適用で作るスケジュールは日単位）
- 通知、リマインダー、メール連携
- 週表示・日表示（月カレンダー以外）
- スケジュールの論理削除の取り消し
- カテゴリの論理削除の取り消し
- ユーザ休日の論理削除の取り消し
- ユーザ休日の年月日・名称を画面から変更すること（削除して同じ日に再登録する）
- 検索、一覧専用画面、印刷、外部カレンダー連携

## 未決事項

- なし

## 承認

現在の状態: 承認済み

| 日時 | 状態 | 変更概要 |
|------|------|----------|
| 2026-08-27 20:40 | 未承認 | 初版 |
| 2026-08-27 20:46 | 未承認 | 機能名 `schedule`・カテゴリ必須を確定。複数日の時間単位は開始日以外タイトルのみ。スマホ詳細の実施状態変更は編集のみ。今日へ戻るを追加 |
| 2026-08-27 20:50 | 未承認 | 同一ユーザの未削除カテゴリで名称の重複を禁止 |
| 2026-08-27 21:04 | 承認済み | 初版を承認。カテゴリ名称の重複禁止を含む |
| 2026-08-27 21:46 | 未承認 | ユーザ休日の登録と、土日・休日の日付色を追加 |
| 2026-08-27 21:55 | 承認済み | ユーザ休日と日付色を承認。日本の祝日はバックエンドで算出（外部APIなし） |
| 2026-08-27 23:37 | 未承認 | ユーザ休日の操作をヘッダの設定ダイアログへ移す。PC ではカレンダーと同時表示しない |
| 2026-08-27 23:46 | 承認済み | ユーザ休日をヘッダの設定ダイアログへ移す変更を承認 |
| 2026-08-29 09:37 | 未承認 | 今日へ戻る操作を削除 |
| 2026-08-29 11:25 | 承認済み | 今日へ戻る操作の削除を承認 |
| 2026-08-29 21:32 | 未承認 | その月の日が無い週は出さない。含む週は前後の月の日も出す |
| 2026-08-29 22:42 | 未承認 | ユーザ休日は日の右クリックで登録・削除。設定ダイアログから祝日登録を外す |
| 2026-08-29 22:52 | 承認済み | ユーザ休日の日右クリック登録・削除、設定ダイアログから祝日登録を外す変更を承認 |
| 2026-08-29 23:08 | 未承認 | ルーチンの登録・削除・指定年月への適用を追加。スケジュールにルーチン識別を追加（初期は無し） |
| 2026-08-29 23:18 | 承認済み | ルーチンの登録・削除・指定年月への適用、スケジュールのルーチン識別を承認 |
| 2026-08-30 00:17 | 未承認 | ルーチンの項目変更（REQ-040）。適用時の年月指定は操作の都度。一覧に年月は置かない |
| 2026-08-30 00:23 | 承認済み | ルーチンの項目変更と適用時の年月指定を承認 |
